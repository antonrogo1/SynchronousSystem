Sample input file Description:

First number number of processes, (determines size of connectivity matrix)
Second line ids of processes separated by comma (Have to much order of connectivity matrix)

3-X - Connectivity matrix. Each line represent process' connections to other processes(columns). -1 means no connection.

IF you decide to create your own Input File make sure that your matrix is symmetric.